## Security Best Practices

### Cryptographic Operations Conventions

This section covers the conventions and best practices for cryptographic operations conventions
within the broader context of Python application security conventions including input sanitization, secret management, and secure coding patterns. Following these guidelines ensures
consistency across the codebase and reduces the likelihood of common errors.

Understanding cryptographic operations conventions is essential for maintaining high-quality Python
code in our projects. The patterns described here have been refined through
extensive use in production systems and reflect industry best practices adapted
to our specific needs.

#### Guidelines for Cryptographic Operations Conventions

The following guidelines govern how cryptographic operations conventions should be implemented and maintained in this project:

1. **Rule 1**: Never trust user input; validate and sanitize all external data at the boundary
2. **Rule 2**: Use parameterized queries exclusively; never interpolate user data into SQL strings
3. **Rule 3**: Store secrets in environment variables or a secret manager, never in code or config files
4. **Rule 4**: Use `secrets` module instead of `random` for security-sensitive token generation
5. **Rule 5**: Apply the principle of least privilege for service accounts and API keys
6. **Rule 6**: Run `pip audit` and `safety check` in CI to detect vulnerable dependencies
7. **Rule 7**: Use `hashlib` with `pbkdf2_hmac` or `bcrypt` for password hashing, never MD5 or SHA1 alone

    #### Code Example

    The following example demonstrates the recommended pattern for
    cryptographic operations conventions:

    ```python
    @dataclass(frozen=True, slots=True)
class CryptographicOperationsConventConfig:
    """Configuration for cryptographic operations conventions.

    Follows the project's security best practices conventions
    for immutable configuration objects.
    """
    name: str
    enabled: bool = True
    max_items: int = 100
    timeout_seconds: float = 30.0
    tags: tuple[str, ...] = ()

    def __post_init__(self) -> None:
        """Validate configuration values."""
        if self.max_items <= 0:
            raise ValueError("max_items must be positive")
        if self.timeout_seconds <= 0:
            raise ValueError("timeout_seconds must be positive")

    @classmethod
    def from_env(cls, prefix: str = "CRYPTOGRAPHIC_OPERATIONS_CONVE") -> "CryptographicOperationsConventConfig":
        """Create configuration from environment variables.

        Args:
            prefix: Environment variable prefix.

        Returns:
            Configured instance.
        """
        import os
        return cls(
            name=os.environ.get(f"{prefix}_NAME", "default"),
            enabled=os.environ.get(f"{prefix}_ENABLED", "true").lower() == "true",
            max_items=int(os.environ.get(f"{prefix}_MAX_ITEMS", "100")),
            timeout_seconds=float(os.environ.get(f"{prefix}_TIMEOUT", "30.0")),
        )
    ```

    **Explanation**: This example follows our project conventions by using
    explicit type annotations, comprehensive docstrings, and proper error
    handling. Note the use of logging instead of print statements, and the
    structured return types that facilitate downstream processing.

#### Best Practices

When working with cryptographic operations conventions in the context of security best practices, keep these practices in mind:

- Never trust user input; validate and sanitize all external data at the boundary
- Use parameterized queries exclusively; never interpolate user data into SQL strings
- Store secrets in environment variables or a secret manager, never in code or config files
- Use `secrets` module instead of `random` for security-sensitive token generation

These practices help maintain code quality and reduce the risk of introducing bugs or performance issues related to cryptographic operations conventions.

#### Common Anti-Patterns

Avoid the following anti-patterns when implementing cryptographic operations conventions:

- **Ignoring error conditions**: Always handle potential failures explicitly
  rather than silently swallowing exceptions or returning None.
- **Missing documentation**: Every public function and class must have a
  docstring explaining its purpose, parameters, and return value.
- **Hardcoded configuration**: Extract configuration values into environment
  variables or configuration files rather than embedding them in code.
- **Insufficient testing**: Write tests that cover both the happy path and
  edge cases, including error conditions and boundary values.

These anti-patterns are commonly encountered during code reviews and should
be addressed before merging any changes related to security best practices.

#### Integration Notes

When integrating cryptographic operations conventions into existing code, ensure that
the surrounding code follows the same conventions described in this
section. Consistency is more important than any individual rule, so
when in doubt, follow the established patterns in the immediate
codebase context.

