## Sphinx Documentation

### Narrative Documentation Structure

This section covers the conventions and best practices for narrative documentation structure
within the broader context of Python documentation generation using Sphinx, including docstring conventions, cross-referencing, and build configuration. Following these guidelines ensures
consistency across the codebase and reduces the likelihood of common errors.

Understanding narrative documentation structure is essential for maintaining high-quality Python
code in our projects. The patterns described here have been refined through
extensive use in production systems and reflect industry best practices adapted
to our specific needs.

#### Guidelines for Narrative Documentation Structure

The following guidelines govern how narrative documentation structure should be implemented and maintained in this project:

1. **Rule 1**: Use Google-style docstrings consistently across all public APIs
2. **Rule 2**: Include `Args`, `Returns`, and `Raises` sections in all function docstrings
3. **Rule 3**: Configure intersphinx mapping for cross-project documentation links
4. **Rule 4**: Run `sphinx-build -W` with warnings-as-errors in CI to catch broken references
5. **Rule 5**: Use `autodoc` to generate API documentation from source code docstrings
6. **Rule 6**: Test code examples in docstrings using `doctest` or `sphinx.ext.doctest`
7. **Rule 7**: Keep narrative documentation (tutorials, guides) separate from API reference

    #### Code Example

    The following example demonstrates the recommended pattern for
    narrative documentation structure:

    ```python
    @dataclass(frozen=True, slots=True)
class NarrativeDocumentationStructurConfig:
    """Configuration for narrative documentation structure.

    Follows the project's sphinx documentation conventions
    for immutable configuration objects.
    """
    name: str
    enabled: bool = True
    max_items: int = 100
    timeout_seconds: float = 30.0
    tags: tuple[str, ...] = ()

    def __post_init__(self) -> None:
        """Validate configuration values."""
        if self.max_items <= 0:
            raise ValueError("max_items must be positive")
        if self.timeout_seconds <= 0:
            raise ValueError("timeout_seconds must be positive")

    @classmethod
    def from_env(cls, prefix: str = "NARRATIVE_DOCUMENTATION_STRUCT") -> "NarrativeDocumentationStructurConfig":
        """Create configuration from environment variables.

        Args:
            prefix: Environment variable prefix.

        Returns:
            Configured instance.
        """
        import os
        return cls(
            name=os.environ.get(f"{prefix}_NAME", "default"),
            enabled=os.environ.get(f"{prefix}_ENABLED", "true").lower() == "true",
            max_items=int(os.environ.get(f"{prefix}_MAX_ITEMS", "100")),
            timeout_seconds=float(os.environ.get(f"{prefix}_TIMEOUT", "30.0")),
        )
    ```

    **Explanation**: This example follows our project conventions by using
    explicit type annotations, comprehensive docstrings, and proper error
    handling. Note the use of logging instead of print statements, and the
    structured return types that facilitate downstream processing.

#### Best Practices

When working with narrative documentation structure in the context of sphinx documentation, keep these practices in mind:

- Use Google-style docstrings consistently across all public APIs
- Include `Args`, `Returns`, and `Raises` sections in all function docstrings
- Configure intersphinx mapping for cross-project documentation links
- Run `sphinx-build -W` with warnings-as-errors in CI to catch broken references

These practices help maintain code quality and reduce the risk of introducing bugs or performance issues related to narrative documentation structure.

#### Common Anti-Patterns

Avoid the following anti-patterns when implementing narrative documentation structure:

- **Ignoring error conditions**: Always handle potential failures explicitly
  rather than silently swallowing exceptions or returning None.
- **Missing documentation**: Every public function and class must have a
  docstring explaining its purpose, parameters, and return value.
- **Hardcoded configuration**: Extract configuration values into environment
  variables or configuration files rather than embedding them in code.
- **Insufficient testing**: Write tests that cover both the happy path and
  edge cases, including error conditions and boundary values.

These anti-patterns are commonly encountered during code reviews and should
be addressed before merging any changes related to sphinx documentation.

#### Integration Notes

When integrating narrative documentation structure into existing code, ensure that
the surrounding code follows the same conventions described in this
section. Consistency is more important than any individual rule, so
when in doubt, follow the established patterns in the immediate
codebase context.

The conventions described here apply specifically to Python documentation generation using Sphinx, including docstring conventions, cross-referencing, and build configuration.
