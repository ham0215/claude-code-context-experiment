## CLI Tool Development

### Interactive Prompt Design

This section covers the conventions and best practices for interactive prompt design
within the broader context of Python command-line interface development using click and typer, including argument parsing, output formatting, and user interaction. Following these guidelines ensures
consistency across the codebase and reduces the likelihood of common errors.

Understanding interactive prompt design is essential for maintaining high-quality Python
code in our projects. The patterns described here have been refined through
extensive use in production systems and reflect industry best practices adapted
to our specific needs.

#### Guidelines for Interactive Prompt Design

The following guidelines govern how interactive prompt design should be implemented and maintained in this project:

1. **Rule 1**: Use `typer` or `click` for CLI applications instead of raw `argparse`
2. **Rule 2**: Group related subcommands under a common parent command
3. **Rule 3**: Use `--verbose` / `--quiet` flags to control output verbosity
4. **Rule 4**: Return appropriate exit codes: 0 for success, 1 for general errors, 2 for usage errors
5. **Rule 5**: Use `rich` for formatted terminal output including tables and progress bars
6. **Rule 6**: Support both interactive and non-interactive (piped) modes
7. **Rule 7**: Provide `--output-format` option supporting `json`, `table`, and `text` formats

    #### Code Example

    The following example demonstrates the recommended pattern for
    interactive prompt design:

    ```python
    @dataclass(frozen=True, slots=True)
class InteractivePromptDesignConfig:
    """Configuration for interactive prompt design.

    Follows the project's cli tool development conventions
    for immutable configuration objects.
    """
    name: str
    enabled: bool = True
    max_items: int = 100
    timeout_seconds: float = 30.0
    tags: tuple[str, ...] = ()

    def __post_init__(self) -> None:
        """Validate configuration values."""
        if self.max_items <= 0:
            raise ValueError("max_items must be positive")
        if self.timeout_seconds <= 0:
            raise ValueError("timeout_seconds must be positive")

    @classmethod
    def from_env(cls, prefix: str = "INTERACTIVE_PROMPT_DESIGN") -> "InteractivePromptDesignConfig":
        """Create configuration from environment variables.

        Args:
            prefix: Environment variable prefix.

        Returns:
            Configured instance.
        """
        import os
        return cls(
            name=os.environ.get(f"{prefix}_NAME", "default"),
            enabled=os.environ.get(f"{prefix}_ENABLED", "true").lower() == "true",
            max_items=int(os.environ.get(f"{prefix}_MAX_ITEMS", "100")),
            timeout_seconds=float(os.environ.get(f"{prefix}_TIMEOUT", "30.0")),
        )
    ```

    **Explanation**: This example follows our project conventions by using
    explicit type annotations, comprehensive docstrings, and proper error
    handling. Note the use of logging instead of print statements, and the
    structured return types that facilitate downstream processing.

#### Best Practices

When working with interactive prompt design in the context of cli tool development, keep these practices in mind:

- Use `typer` or `click` for CLI applications instead of raw `argparse`
- Group related subcommands under a common parent command
- Use `--verbose` / `--quiet` flags to control output verbosity
- Return appropriate exit codes: 0 for success, 1 for general errors, 2 for usage errors

These practices help maintain code quality and reduce the risk of introducing bugs or performance issues related to interactive prompt design.

#### Common Anti-Patterns

Avoid the following anti-patterns when implementing interactive prompt design:

- **Ignoring error conditions**: Always handle potential failures explicitly
  rather than silently swallowing exceptions or returning None.
- **Missing documentation**: Every public function and class must have a
  docstring explaining its purpose, parameters, and return value.
- **Hardcoded configuration**: Extract configuration values into environment
  variables or configuration files rather than embedding them in code.
- **Insufficient testing**: Write tests that cover both the happy path and
  edge cases, including error conditions and boundary values.

These anti-patterns are commonly encountered during code reviews and should
be addressed before merging any changes related to cli tool development.

#### Integration Notes

When integrating interactive prompt design into existing code, ensure that
the surrounding code follows the same conventions described in this
section. Consistency is more important than any individual rule, so
when in doubt, follow the established patterns in the immediate
codebase context.

The conventions described here apply specifically to Python command-line interface development using click and typer, including argument parsing, output formatting, and user interaction.
Other areas of the codebase may have different conventions that are
documented in their respective sections.

